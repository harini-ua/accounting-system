<div class="checkbox-filter" data-url="{{ $url }}" data-table="{{ $table }}">
    <label>
        <input name="{{ $name }}" type="checkbox" value="1" />
        <span>{{ $title }}</span>
    </label>
</div>

<div class="checkbox-filter display-flex align-items-center flex-grow-1" data-url="{{ $url }}" data-table="{{ $table }}">
    <label>
        <input name="{{ $name }}" type="checkbox" value="1" />
        <span>{{ $title }}</span>
    </label>
</div>
